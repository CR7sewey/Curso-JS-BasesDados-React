const nome = "Miguel"; // String
// const nome; error
// nome = "Ze"; error
console.log(nome);

const firstNum = 3; // number - tipagem dinamica - sabe que é um numero
const secondNum = 5; // number
const conta = firstNum * secondNum; // number
let contaDobro = conta * 2; // 30
contaDobroMaisUm += 1; // 31 = 30 + 1; contaDobro++; contaDobro = contaDobro + 1
console.log(contaDobroMaisUm, typeof contaDobroMaisUm, typeof nome);

/**
 * Nota:
 * String = "Text" | Number = Número
 */
