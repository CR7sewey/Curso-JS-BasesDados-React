// Constroi uma função que recebe 2 números e retornem o maior deles

function oMaiorNumero(num1, num2) {
  /**
   * Pistas: Operação ternária ou if/else
   */
  return; //algo; o que é devolvido pela funcao
}
console.log(oMaiorNumero(110, 1));

// arrow function
const oMaiorNumero2 = (num1, num2) => {
  /**
   * Pistas: Operação ternária ou if/else
   */
  return; //algo;
};
console.log(oMaiorNumero2(0, 1));
